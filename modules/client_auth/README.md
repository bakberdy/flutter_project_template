# client_auth

Client authentication feature module. It owns login and verification flows, authentication data and domain logic, navigation, localization, DI, constants, endpoints, and module assets.

Profile, sessions, account status, and preferences do not belong to this module. The package exposes only localization delegates, DI registration, and generated route contracts.

