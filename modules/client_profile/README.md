# client_profile

Client profile feature module. It owns profile creation and editing, the current-user state, avatar operations, sessions, and account-status screens.

Feature areas are split into `profile`, `sessions`, and `account_status`. Shared module endpoints, constants, navigation, localization, DI, and assets live under `src/common` and remain internal unless explicitly exported.

