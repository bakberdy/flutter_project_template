# admin_users

Admin user-management feature module. It owns the users list and user details flows, their domain and data layers, navigation, localization, DI, constants, endpoints, and module assets.

The package exposes only localization delegates, DI registration, and generated route contracts. Internal configuration and context extensions are not public API.

