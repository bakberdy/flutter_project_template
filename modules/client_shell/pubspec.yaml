name: client_shell
description: Client authorized shell and main navigation module.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter

  auto_route: ^11.1.0
  core:
    path: ../core
  design_system:
    path: ../design_system
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator: ^10.5.0
  build_runner: ^2.13.0
  flutter_lints: ^6.0.0
  index_generator: ^4.1.2

flutter:
  generate: true
