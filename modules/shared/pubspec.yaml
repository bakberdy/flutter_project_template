name: shared
description: Shared presentation integrations and reusable application utilities.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter

  auto_route: ^11.1.0
  core:
    path: ../core
  design_system:
    path: ../design_system
  equatable: ^2.0.8
  flutter_bloc: ^9.1.1
  get_it: ^9.2.0
  intl: ^0.20.2
  json_annotation: ^4.12.0
  inspector: ^4.0.0

dev_dependencies:
  build_runner: ^2.13.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.13.0

flutter:
  generate: true
